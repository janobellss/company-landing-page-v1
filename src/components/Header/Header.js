import React from 'react';

const Header = () => {
    return(
        <header className='site-header'>
            <div className='site-header__container container'>
                <a href='#' className='site-header__logo'>gtd.</a>
            </div>
        </header>
    );
}

export default Header;